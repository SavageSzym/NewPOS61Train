/*
	Configuration file for way station front-end.
*/
var gConfig = {
	/* Verification interval for updates on POS list */
	"REFRESH_INTERVAL": 20000,
	/* URL for retrieving screen.xml */
	"SCREEN_XML_URL": "screen.xml",
	/* Screen timeout for screens with timeout="true" */
	"SCREEN_TIMEOUT": 20000,
	/* Max time that an information message will be displayed */
	"MESSAGES_TIMEOUT": 10000,
	/* Base screen number */
	"BASE_SCREEN": 2001
}